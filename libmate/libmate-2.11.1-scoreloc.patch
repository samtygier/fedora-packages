--- libmate-1.1.3/libmate/mate-score.c	2012-01-26 14:46:16.000000000 +0100
+++ libmate-1.1.3/libmate/mate-score.c.scoreloc	2012-02-15 21:56:34.019940956 +0100
@@ -91,11 +91,11 @@ static gchar* mate_get_score_file_name(c
 {
 	if (level)
 	{
-		return g_strconcat(LIBMATE_LOCALSTATEDIR, G_DIR_SEPARATOR_S "games" G_DIR_SEPARATOR_S, progname, ".", level, ".scores", NULL);
+		return g_strconcat(LIBMATE_LOCALSTATEDIR, G_DIR_SEPARATOR_S "lib" G_DIR_SEPARATOR_S "games" G_DIR_SEPARATOR_S, progname, ".", level, ".scores", NULL);
 	}
 	else
 	{
-		return g_strconcat(LIBMATE_LOCALSTATEDIR, G_DIR_SEPARATOR_S "games" G_DIR_SEPARATOR_S, progname, ".scores", NULL);
+		return g_strconcat(LIBMATE_LOCALSTATEDIR, G_DIR_SEPARATOR_S "lib" G_DIR_SEPARATOR_S "games" G_DIR_SEPARATOR_S, progname, ".scores", NULL);
 	}
 }
 
